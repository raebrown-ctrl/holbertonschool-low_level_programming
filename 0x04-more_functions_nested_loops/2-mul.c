#include "main.h"

/**
 * mul - multiplies two integers
 * @a: the first number to mulitply
 * @b: the second number to mulitply with first number
 *
 * Return: the result of multiplication between x param and y param
 */
int mul(int a, int b)
{
	return (a * b);
}
